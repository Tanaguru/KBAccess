<%@ page pageEncoding="UTF-8" %>
<ol>
    <c:forEach var="criterion" items="${criterionStatistics}">
        <li>
            <a href="<c:url value='/testcase/list.html?criterion=${criterion.id}'/>"
               title="Lister les testcases du critère ${criterion.code}">${criterion.code}</a>
            (${criterion.testcaseCount} testcase<c:if test="${criterion.testcaseCount > 1}">s</c:if>)
        </li>
    </c:forEach>
</ol>
